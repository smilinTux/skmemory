[Unit]
Description=Run skmemory sync for %i every 6h

[Timer]
# First run 5 min after boot, then every 6 hours
OnBootSec=5min
OnUnitActiveSec=6h
# If the system was off when a tick was due, run on next boot
Persistent=true
Unit=skmemory-sync@%i.service

[Install]
WantedBy=timers.target
